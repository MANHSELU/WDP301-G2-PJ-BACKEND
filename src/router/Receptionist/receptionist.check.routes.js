const express = require("express");
const routerLeTanCheck = express.Router();
const LeTanController = require("../../controller/Assistant Driver/assistantDriver.controller");
// routerLeTanCheck.get("/trips", LeTanController.getAssistantTrips);
module.exports = routerUserCheck;
